# Architecture Document
 
## 1. Introduction
이 문서는 Spider의 전체 구조가 어떤 식으로 구성되어 있는지 정리한 문서다.
 
개발 초기 단계에서 로버트 C. 마틴의 클린 아키텍처를 읽으며 해당 책의 내용을 실제 개발에 적용하기 위해 노력했다.
특히 객체 지향 설계 원칙 SOLID에서 OCP와 DIP를, 컴포넌트 결합 원칙에서는 ADP를 많이 신경썼다.
 
개발 도중 변경이 잦을 수 있음을 감안해 최대한 변경과 확장이 용이하도록 설계했으며 클래스 간 의존성도 가능한 한 한쪽 방향으로 흐르게끔 만들었다.
 
---
 
## 2. High-Level Architecture
Spider에는 **GameManager**를 비롯한 다수의 **~Manager**로 명명된 클래스가 존재하며 해당 클래스들의 상호작용으로 여러 게임 요소들이 작동한다.
 
- **GameManager**  
  코드 구조의 최상단에 존재하며 다른 **~Manager** 클래스들과 **Map**을 초기화한다.
- **Map**, **RandomSphereMap**
  기둥과 다리로 구성된 맵을 절차적으로 생성한다. **Map** 은 추상 클래스이며 실제 맵 생성 구현은 **Map**을 상속한 클래스에서 수행한다. 현재는 **RandomSphereMap**이 사용되고 있다. 개발 도중에 맵의 구조가 평면에서 구형으로 바뀌는 등 몇 차례 수정이 있었다. 격자형 맵이나 테스트용 맵 또한 **Map**을 상속해 만들었으나 지금은 불필요하여 삭제되었다.
- **GameStateManager**  
  게임의 턴(플레이어 턴인가 적 턴인가), 게임의 상태(특수 행동이 사용 중인가 등), UI의 상태(어떤 UI가 띄워져야 하는가)를 관리한다. "On~Change"라는 이름의 이벤트들을 포함하고 있으며 다른 클래스들이 구독해 상태의 변화에 반응할 수 있도록 했다.
- **InputManager**  
  마우스와 키보드 입력을 받아들여 다른 클래스로 넘긴다. 이벤트를 만들어 구독하도록 했다. 마우스 클릭의 경우 어떤 유저가 어떤 오브젝트를 클릭하고자 의도했는지를 계산해 해당 오브젝트를 함께 넘긴다.
- **AudioManager**  
  게임 내에 사용되는 여러 BGM, SFX 등을 통합해 관리한다.
- **FacilityManager**  
  시설의 설치와 제거를 담당한다. 사용 가능한 시설의 정보를 유니티 에디터 상에서 쉽게 입력할 수 있도록 만들었다.
- **SpecialActionManager**  
  특수 행동의 추가와 제거를 담당한다. 특수 행동이 여러 개 사용 가능할 경우 특수 행동 키 입력 시 어떤 것이 사용될지도 관리한다.
- **UiManager**  
  Ui의 활성화와 비활성화를 제어한다. 어떤 UI 상태에 어떤 패널을 띄울지를 저장하며 **GameStateManager**에서 UI 상태의 변화가 발생한 경우 해당 변화를 UI에 적용한다.
- **UnitSystem**, **UnitManager**, **EnemyWaveManager**
  
  게임 내 존재하는 유닛들(플레이어, 적)의 생성과 삭제, 이동 등을 관리한다
  **UnitSystem**이 **UnitManager**와 **EnemyWaveManager**를 참조해 사용한다.
  **UnitManager**는 맵 위에 유닛을 생성하고 삭제하는 작업을 보다 낮은 단계에서 수행한다.
  **EnemyWaveManager**는 게임의 난이도 커브를 관리하며 턴이 지남에 따라 적들의 종류와 수가 어떻게 변해야 하는지를 저장하고 관리한다.
  **UnitSystem**은 유닛의 생성, 삭제, 이동을 보다 높은 단계에서 수행하며 플레이어 턴과 적 턴에 수행되어야 하는 일들을 관리한다.
---
 
## 3. Map Generation System
 
거미줄과 같이 정점과 간선으로 이루어진 맵을 만들고자 했다. 그래서 기둥과 다리로 맵이 구성되게 했다.
평면형 맵의 경우 플레이어가 적으로부터 도주하다 벽에 막혀서 더 이상 도망치지 못하는 경우가 자주 나왔고, 난이도 조절이 어려웠다. 그래서 맵을 구형으로 생성하게 만들었다.
 
### Architecture
**Pillar**와 **Bridge**는 **Node** 클래스를 상속한다. **Node**는 그 위에 유닛이 올라갈 수 있다. **Pillar**는 그 위에 시설이 지어질 수 있다.
**Map** 추상 클래스는 **Pillar**와 **Bridge**의 생성과 파괴, 연결 기능을 제공하며 BFS 기반 경로 탐색과 거리 측정 등 맵과 관련된 알고리즘들을 포함하고 있다.
**RandomSphereMap** 클래스는 **Map** 추상 클래스를 상속하며, 구형 맵을 랜덤하게 생성하는 알고리즘이 구현되어 있다.
 
### Generation Steps
#### 1) 기둥 생성
큰 기둥과 작은 기둥을 구형으로 생성한다.
기둥 간에 최소한의 간격을 유지하기 위해 포아송 샘플링(Poisson sampling)을 이용했다.
큰 기둥은 30°, 작은 기둥은 15° 이상의 기둥 간 각도를 가지게끔 했다.
 
큰 기둥을 먼저 생성한 뒤, 작은 기둥은 큰 기둥이 이미 존재한다는 가정하에 생성했다.  
이 방식 덕분에 큰 기둥이 자연스럽게 맵의 주요 구조를 형성하고 작은 기둥들이 그 사이를 메우는 형태가 되었다.
 
기둥의 회전은 구 표면의 법선 벡터를 기준으로 설정하여  
각 기둥이 구 표면에 붙어 있는 것처럼 보이도록 했다.
 
#### 2) 다리 생성
모든 기둥 쌍에 대해 두 기둥을 연결하는 다리를 만들었다.
구 중심과 너무 가까운 다리는 제거해 다리가 구의 겉부분에 형성되게 했다.
 
#### 3) 교차 제거
가능한 모든 다리 쌍에 대해 서로 교차하는지를 검사해 교차 횟수를 기록한 뒤, 교차 횟수가 많은 다리부터 제거하는 방식으로 정리하되 최대한 많은 다리를 남기도록 했다.
일종의 Greedy 알고리즘이 사용되었다고 할 수 있다.
 
3차원 상에서 두 직선의 교차 여부를 판정하기 어려웠기 때문에 두 다리 주변의 국소 평면에 투영한 뒤 2D 교차 판정을 수행하는 방식으로 해결했다.
 
이 과정 덕분에 지저분하게 얽히지 않은 깔끔한 그래프 구조의 맵이 생성되었다.
 
---
 
## 4. Unit System
 
Spider에선 맵 위에서 플레이어와 적이 움직이며 상호작용한다.
이들은 서로 공통된 기능을 가지고 있으므로 중복을 최소화하고 관리를 편히 하기 위해 상속 구조를 활용하였다.
 
플레이어와 적이 모두 **Unit** 추상 클래스를 상속하는 구조로 되어 있다. 
유닛 간 공통 동작, 즉 **Pillar**와 **Bridge**로 구성된 맵 상에 생성, 이동, 파괴되기 위한 기능을 **Unit**이 담고 있다.
 
Enemy는 추가로 다음과 같은 상속 계층을 가진다.
 
- **Enemy**
  - **Spawner**: 다른 적을 생성할 수 있는 적
  - **Boss**: 특수한 행동 패턴을 가진 강력한 적
  - **Mover**: 플레이어를 추적하며 이동할 수 있는 적
    - **Pursuer**: 가장 기본적인 적
    - **Runner**: 이동 속도가 빠른 적
    - **Corroder**: 지나간 다리를 파괴하며 이동하는 적
이 계층 구조 덕분에 다양한 적을 만들면서도 공통 동작은 재사용할 수 있었다.
이후 새로운 적의 추가와 같은 확장에도 열려있도록 했다.
 
**Unit**에서 **UnitSystem**에 이르기까지 유닛의 이동과 행동에 관련된 함수들을 유니티의 코루틴 시스템을 이용해 비동기 처리하였다.
이를 통해 여러 유닛이 동시에 움직일 수 있게끔 했다.
 
적들의 AI는 플레이어가 일정 거리 이내에 감지되면 추적을 시작해 플레이어를 향한 최단 경로로 이동하는 것이다.
또한 추적을 시작한 때부터 일정 턴이 경과하면 소멸하게 했다.
 
---
 
### Architecture
#### **UnitManager**
유닛의 생성과 삭제를 담당한다.  
**UnitManager**는 현재 생성되어 있는 유닛들을 리스트로 관리하며 유닛 조작을 위한 다양한 유틸리티 함수들을 가지고 있다.  
유닛을 조작하고자 하는 다른 모든 클래스들은 유닛을 직접 조작하는 대신 **UnitManager**를 통해 조작해야 한다.
이러한 계층 분리를 통해 버그의 발생 가능성을 줄이며 코드의 수정 용이성을 늘렸다.
 
---
 
#### EnemyWaveManager  
적 생성과 관련된 난이도 커브를 담당한다.  
현재 턴 수에 따라 어떤 적을 어떤 비율로 추가해야 하는지를 저장하고 있으며 UnitSystem이 매 턴마다 EnemyWaveManager를 조회해 필요한 적을 스폰하도록 한다.
유니티에서 제공하는 ScriptableObject 클래스를 상속하는 **EnemyWaveData** 클래스를 만들어 적 스폰과 관련된 데이터를 파일 형태로 유니티 에디터에서 쉽게 수정하고 저장할 수 있게 했다.
 
---
 
#### UnitSystem  
**UnitSystem**은 턴 기반 유닛 동작의 중심 역할을 한다.  
**GameStateManager**의 이벤트를 구독해 턴의 변화를 인식하고 플레이어와 적이 적절한 행동을 취하도록 한다.
이때 유닛의 조작은 **UnitManager**를 이용해 이루어진다.
적의 스폰 종류와 수는 **EnemyWaveManager**로부터 받아와 사용한다.
 
---
 
## 5. Facility System
 
시설은 게임의 로그라이트성 요소로, 기둥 위에 설치해서 여러 이득을 얻을 수 있다.
플레이어는 스포너를 파괴하거나 죽은 적이 드랍한 구슬을 먹어 경험치를 얻을 수 있으며, 경험치가 다 차면 레벨이 오르며 시설을 하나 선택해 지을 수 있다.
시설은 일반적으로 큰 기둥에만 설치할 수 있으나 업그레이드 시설은 작은 기둥에만 설치할 수 있다. 업그레이드 시설은 인접한 시설을 강화한다. 따라서 시설을 적절히 배치하여 최대한의 업그레이드를 얻는 심시티 또한 게임의 전략적 요소이다.
 
---
 
### Architecture
**Facility** 추상 클래스는 시설 하나를 나타내며 자신이 설치된 **Pillar**를 참조한다.
**MaxHpFacility** 등 구체적인 클래스는 **Facility**를 상속한다.
**FacilityManager** 클래스는 현재 존재하는 시설을 리스트 형태로 관리하며 시설의 설치와 파괴 함수를 다른 클래스에 제공한다.
**FacilityData**는 ScriptableObject를 상속해 시설에 대한 정보를 파일 형태로 에디터에서 조작할 수 있게 한 클래스다.
 
---
 
## 6. Special Action System
 
특수 행동은 도주에 도움이 되는 특수한 행동으로, 플레이어가 이동 대신 선택할 수 있다. 특수 행동 시 스태미나를 소모하며 턴이 종료된다.  
특수 행동이 존재하지 않는 경우 적으로부터 포위되어 도저히 빠져나올 수 없는 경우가 있기에 추가한 시스템이다.
특수 행동을 쓰기 위해서는 해당 특수 행동을 해금하는 시설을 건설해야 하며, 그 시설을 업그레이드하면 특수 행동이 강화된다.
 
---
 
### Architecture
**SpecialAction** 추상 클래스는 특수 행동 클래스들의 기본이 되며 특수 행동의 활성화와 비활성화, 키 입력 처리 등의 틀을 제공한다.
**SpecialActionManager**는 현재 사용 가능한 특수 행동들을 관리하며 특수 행동이 여러 개 존재할 가능성을 고려해 선택 교체와 여러 행동의 등록/해제를 지원하도록 했다.
 
---
 
## 7. UI System
 
유니티에서 제공하는 UI 관련 객체 중 Panel을 중심으로 동작을 제어하도록 구조를 짰다.
깔끔하고 동적인 반응을 제공하기 위해 유니티 에셋 중 Modern UI Pack을 이용했다. 텍스트 폰트도 이에 맞추어 결정했다.
 
---
 
### Architecture
**Panel** 추상 클래스는 버튼과 텍스트, 이미지들이 모여 있으며 한 번에 활성화, 비활성화 할 수 있는 단위이다. 해당 클래스를 상속해 구체적인 패널들이 만들어진다.
**UiManager**는 패널들의 목록을 관리하며 패널의 활성화와 비활성화 여부를 결정한다. **GameStateManager**의 UiState 관련 이벤트를 구독해 UiState의 변경이 발생했을 시 적절한 UI 전환이 이루어지게 한다.
 
---
 
## 8. Game State System
 
FSM(Finite State Machine) 개념을 이용해 상태 기반으로 게임을 설계했다. 한 종류의 상태만으로는 게임이 요하는 다양한 상태를 표현하기에 부족하다 판단해 총 세 개 종류의 상태를 이용하기로 했다.
지금이 플레이어 턴인지, 적 턴인지를 의미하는 CurrentTurn, 게임의 전반적인 상태를 의미하며 대개 입력을 어떤 클래스가 처리할지를 결정하는 CurrentGameState, 어떤 UI가 활성화되어야 하는지를 결정하는 CurrentUiState가 열거형 프로퍼티로 존재한다.
해당 프로퍼티는 private set으로 정의되어 외부에서 직접 할당할 수 없고 **GameStateManager**가 제공하는 함수를 통해서만 제어할 수 있게 해 안정성을 높였다.
UiState를 설정할 때는 object를 하나 추가로 넘길 수 있게 하여 UIManager에 추가적인 정보를 제공할 수 있게 했다.
 
---
 
## 9. Visual Effects
 
Spider의 시각 효과는 최소한의 구성으로 유지했다.
시설 고리의 깜빡임, 특수 행동 UI 강조 등은 Animator와 MaterialPropertyBlock(MPB)을 조합해 구현했다.  
또한 맵 중심이 어둠처럼 보이도록 하기 위해 기둥에 무광 그라데이션이 들어간 전용 메테리얼이 필요했고, 이를 위해 간단한 셰이더 그래프를 제작해 사용했다.
적이 활성화된 상태임을 나타내기 위해 파티클 이펙트를 사용했다.
 
---
 
## 10. Class Diagram
<p align="center"><img src="Class Diagram/HighLevel.png" width="80%"></p>
<p align="center"><img src="Class Diagram/Map.png" width="80%"></p>
<p align="center"><img src="Class Diagram/Unit.png" width="80%"></p>
<p align="center"><img src="Class Diagram/Facility.png" width="80%"></p>
<p align="center"><img src="Class Diagram/SpecialAction.png" width="80%"></p>
<p align="center"><img src="Class Diagram/UI.png" width="80%"></p>
